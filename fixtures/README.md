# Sanitized behavior fixtures

`fixtures/` 用于保存不含账号、设备标识、Cookie、Token、定位和私人内容的响应样本。

新增真实流量样本前必须：

1. 删除请求头、用户 ID、设备 ID、IP、经纬度和业务 Token。
2. 用固定占位符替换评论、昵称等私人内容。
3. 同时记录命中 URL 路径、是否加密以及期望保留/删除的字段。
4. 原版和优化版必须使用同一份样本比较输出与存储副作用。

当前测试包含人工构造的边界样本；真实 Egern 流量仍需在设备端脱敏后补充。
